server:
  port: 8082

spring:
  application:
    name: SERVICE-CAR
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery: SERVICE-CAR
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:carservice
    username: "sa"
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

